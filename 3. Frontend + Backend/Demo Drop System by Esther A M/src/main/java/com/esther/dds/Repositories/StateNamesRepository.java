package com.esther.dds.Repositories;

import com.esther.dds.domain.StateNames;
import org.springframework.data.jpa.repository.JpaRepository;

public interface StateNamesRepository extends JpaRepository <StateNames, Long> {
}
